### menus/

VERIFICATION review of menus/ cluster. SoT = MRT `packages/material-react-table/src/components/menus/`. SRT = `apps/test-shadcn/src/components/ui/shadcn-react-table/menus/`. MUI Menu→shadcn DropdownMenu is an accepted deviation. All 7 pairs present.

Recurring accepted pattern (not re-flagged per file): MRT's `<Menu anchorEl … MenuListProps={{dense, sx:{backgroundColor: menuBackgroundColor}}}>` becomes SRT's `<DropdownMenu open={!!anchorEl}>` + a `position:fixed` zero-size span trigger placed at `anchorEl.getBoundingClientRect()`. `menuBackgroundColor` / `mrtTheme` is intentionally dropped (theming handled by shadcn tokens). `density==='compact'` is propagated via an explicit `dense` prop instead of MenuListProps.dense.

### [ ] SRT_ActionMenuItem.tsx : MRT_ActionMenuItem.tsx
- `parity-ok` — icon + label + optional submenu arrow rendered; submenu arrow icon read from `table.options.icons.ArrowRightIcon` exactly as MRT.
- `note` — SRT adds props MRT lacks here (`dense`, `divider` → trailing `DropdownMenuSeparator`, `selected`, `value`/`data-value`). These are required by FilterOptionMenu/ColumnActionMenu callers (MRT passes `divider`/`selected`/`value` to MenuItem via `...rest`); so this is faithful consolidation, not scope creep.
- `note` — `onSelect` is intercepted: when `onOpenSubMenu` is set, `event.preventDefault()` keeps the parent menu open (Radix closes on select by default). Reasonable shadcn-specific adaptation with no MRT equivalent; correct behavior.
- `deviation` (minor a11y) — MRT `MenuItem tabIndex={0}`; SRT submenu-arrow `<button>` has `tabIndex={-1}` (the DropdownMenuItem itself remains focusable via Radix). The arrow is keyboard-unreachable, but in MRT the arrow IconButton is also not separately tab-focusable in practice. Low impact.

### [ ] SRT_CellActionMenu.tsx : MRT_CellActionMenu.tsx
- `gap` (icons not from registry) — SRT imports `CopyIcon, EditIcon` directly from `lucide-react` and renders `<CopyIcon/>`/`<EditIcon/>`. MRT reads `icons: { ContentCopy, EditIcon }` from `table.options.icons`. Task explicitly requires icons read from `table.options.icons`; the copy icon in particular is hardcoded rather than `icons.ContentCopy`. User icon overrides will not apply to this menu. (ColumnActionMenu/RowActionMenu do read from the registry, so this is inconsistent within the cluster.)
- `parity-ok` — both internal items present with identical enable conditions: copy item gated on `enableClickToCopy === 'context-menu'` OR `columnDef.enableClickToCopy === 'context-menu'` (via `parseFromValuesOrFunc`); edit item gated on `enableEditing(row)` && `editDisplayMode === 'cell'`. Labels `localization.copy` / `localization.edit`.
- `parity-ok` — render-callback precedence matches: `columnDef.renderCellActionMenuItems?.(props) ?? renderCellActionMenuItems?.(props)`; `renderActionProps` carries the same keys (cell, closeMenu, column, internalMenuItems, row, table). Empty-guard (`!menuItems?.length && !internalMenuItems?.length` → null) matches MRT's conditional render.
- `note` — internal items are NOT passed `dense` (no `dense={...}` on the SRT_ActionMenuItem children). MRT applied density via MenuListProps.dense to all items; here cell-menu items always render at default padding regardless of `density==='compact'`. Same omission as FilterOptionMenu. (SRT also reads `getState()` without destructuring `density`, so the value is unused.)
- `note` — SRT adds optional local-control props (`anchorEl`/`cell`/`setAnchorEl`) on top of the MRT actionCell-state path; faithful superset, default path matches MRT.

### [ ] SRT_ColumnActionMenu.tsx : MRT_ColumnActionMenu.tsx
- `parity-ok` — all 13 internal items present with identical keys 0–12 and identical enable gating: sort group (`enableSorting && getCanSort`, clear-sort gated on `enableSortingRemoval !== false`), filter group (`enableColumnFilters && getCanFilter`, subheader filter-by item gated on `columnFilterDisplayMode === 'subheader'`), grouping (`enableGrouping && getCanGroup`), pinning (`enableColumnPinning && getCanPin`, 3 items), resize (`enableColumnResizing && getCanResize`), hiding (`enableHiding`, 2 items). All `disabled` predicates match line-for-line (sorted-state, filter-value emptiness, pin direction, columnSizing undefined, getCanHide, all-visible).
- `parity-ok` — localization + `{column}` replacement identical (clearSort, sortByColumnAsc/Desc, clearFilter, filterByColumn, group/ungroupByColumn, pinToLeft/Right, unpin, resetColumnSize, hideColumn, showAllColumns). `divider` placement matches MRT (sort-desc, filter-by, group, unpin, hide-all boundaries).
- `parity-ok` (registry icons + dense — refactor confirmed) — all icons read from `table.options.icons` (ClearAll, Sort, FilterListOff/List, DynamicFeed, PushPin, RestartAlt, VisibilityOff, ViewColumn) with same transforms (Sort asc `rotate(180deg) scaleX(-1)`, PushPin left `rotate(90deg)` / right `rotate(-90deg)`). `dense={density==='compact'}` passed on EVERY SRT_ActionMenuItem. Task's refactor note verified correct.
- `parity-ok` — filter-mode submenu: `showFilterModeSubMenu` computed identically (`enableColumnFilterModes && columnDef.enableColumnFilterModes !== false && !isSelectFilter && (allowed===undefined || allowed.length)`); `onClick`/`onOpenSubMenu` wiring and nested `SRT_FilterOptionMenu` (key 5) match. `handleGroupByColumn` prepends `'srt-row-expand'` (vs MRT `'mrt-row-expand'`) — correct namespace rename.
- `parity-ok` — render-callback precedence identical: `columnDef.renderColumnActionsMenuItems?.(...) ?? renderColumnActionsMenuItems?.(...) ?? internalColumnMenuItems`, payload `{closeMenu, column, internalColumnMenuItems, table}` matches.

### [ ] SRT_FilterOptionMenu.tsx : MRT_FilterOptionMenu.tsx
- `parity-ok` — `srtFilterOptions(localization)` is a byte-for-byte port of `mrtFilterOptions`: all 14 options, same order, same symbols (≈ * a z = ≠ ⇿ ⬌ > ≥ < ≤ ∅ !∅) and same `divider` booleans. Module constants `rangeModes`/`emptyModes`/`arrModes`/`rangeVariants` identical.
- `parity-ok` — `handleSelectFilterMode` logic ported verbatim: global vs column branch, empty/array/range/single value reconciliation (` ` sentinel for empty modes, `[]` for array, `['', '']` for range, value reset on mode change). `allowedColumnFilterOptions` range-variant override identical. `internalFilterOptions` useMemo filter (column: allowed-or-undefined; global: globalFilterModeOptions ∩ contains/fuzzy/startsWith) matches.
- `parity-ok` — render-callback slots present with identical precedence: column path `columnDef.renderColumnFilterModeMenuItems ?? renderColumnFilterModeMenuItems`, global path `renderGlobalFilterModeMenuItems`, fallthrough to mapped internal options. Payload keys (column, internalFilterOptions, onSelectFilterMode, table) match. `selected` / `value` passed through to SRT_ActionMenuItem.
- `note` — items not passed `dense`. MRT destructures `density` and applies via MenuListProps.dense; SRT drops the `density` destructure entirely and omits `dense` on each SRT_ActionMenuItem, so filter-mode list ignores compact density. Consistent with CellActionMenu omission; low visual impact.
- `note` — MRT uses `anchorOrigin={{horizontal:'right', vertical:'center'}}` (submenu opens to the right of anchor). SRT approximates with `DropdownMenuContent side="right" align="start"`. Accepted Menu→DropdownMenu positioning deviation; intent preserved.

### [ ] SRT_RowActionMenu.tsx : MRT_RowActionMenu.tsx
- `parity-ok` — edit item gating identical: `parseFromValuesOrFunc(enableEditing, row) && ['modal','row'].includes(editDisplayMode)`, icon `EditIcon` from `table.options.icons`, label `localization.edit`, `onClick={handleEdit}`.
- `parity-ok` — `renderRowActionMenuItems` slot present with same payload `{closeMenu, row, staticRowIndex, table}`; items appended after edit; `if (!menuItems.length) return null` guard matches MRT.
- `parity-ok` (dense) — SRT passes `dense={density==='compact'}` to the edit SRT_ActionMenuItem and adds `density` to the useMemo deps (MRT deps lacked it but used MenuListProps). Density honored; correct.
- `note` — useMemo dep arrays differ slightly (SRT adds `density`); behaviorally a superset, no parity concern.

### [ ] SRT_ShowHideColumnsMenu.tsx : MRT_ShowHideColumnsMenu.tsx
- `parity-ok` — all four header buttons present with identical gating: hideAll (`enableHiding`, disabled `!getIsSomeColumnsVisible`), resetOrder (`enableColumnOrdering`, disabled `!hasColumnOrderChanged`, calls `getDefaultColumnOrderIds(options, true)`), unpinAll (`enableColumnPinning`, disabled `!getIsSomeColumnsPinned`, `resetColumnPinning(true)`), showAll (`enableHiding`, disabled `getIsAllColumnsVisible`). Labels from localization. MUI `Button`→shadcn `Button variant="ghost" size="sm"` (accepted).
- `parity-ok` — `allColumns` useMemo (left + ordered-center + right leaf reconstruction, group-column bypass), `isNestedColumns`, `hasColumnOrderChanged`, and `hoveredColumn` state ported identically. `handleToggleAllColumns` (filter `enableHiding !== false`, toggle each leaf) matches.
- `parity-ok` — `<Divider/>`→`<DropdownMenuSeparator/>`; per-column rows mapped through `SRT_ShowHideColumnsMenuItems` with same key `${index}-${column.id}`. SRT additionally threads `dense={density==='compact'}` down to the items (MRT relied on MenuListProps.dense).
- `note` — `isSubMenu` prop declared in both interfaces but unused in both (dead prop carried over faithfully).

### [ ] SRT_ShowHideColumnsMenuItems.tsx : MRT_ShowHideColumnsMenuItems.tsx
- `parity-ok` — recursion over `column.columns`, early return on `!columnDef.header || visibleInShowHideMenu === false`, group-column toggle-all-children logic, native DnD (`handleDragStart`/`End`/`Enter`, `reorderColumn` + pin reconciliation), and grab-handle / pinning-button slots (`SRT_GrabHandleButton`, `SRT_ColumnPinningButtons`) all match with same enable gating and same spacer fallbacks (`w-7` ≈ 28px, `w-[70px]`).
- `deviation` (Switch→Checkbox) — MRT renders `FormControlLabel + Switch` inside a `Tooltip`; SRT renders a shadcn `Checkbox` inside `Tooltip*` with a `<label>`. Toggle semantics and `localization.toggleVisibility` tooltip preserved; `disabled={!column.getCanHide()}` matches. Control-type change is within the accepted MUI→shadcn deviation but worth flagging (Switch vs Checkbox affords differently).
- `deviation` (indent halved) — MRT `pl: (column.depth + 0.5) * 2 rem`; SRT `paddingLeft: (column.depth + 0.5) * 1 rem`. Nested-column indentation is half MRT's. Visual-only; verify nested-group menus still read clearly.
- `parity-ok` (dense) — SRT honors `dense` prop (`py-1` vs `py-1.5`) and threads it through recursion; matches MRT MenuListProps.dense intent.
- `note` — drag outline colors mapped from theme (`grey[500]`/`draggingBorderColor`) to tokens (`--muted-foreground`/`--primary`); display-column dimming `opacity-50` preserved (MRT `opacity: columnDefType !== 'display' ? 1 : 0.5`). Accepted theming deviation.
